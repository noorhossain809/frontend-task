/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // images: { domains: ['https://res.cloudinary.com/dsbiemkpu/image/upload/v1712565824'], },
   images: { domains: ['res.cloudinary.com'], },
};

export default nextConfig;
